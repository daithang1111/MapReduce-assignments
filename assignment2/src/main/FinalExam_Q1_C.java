public class FinalExam_Q1_C {
  public static void main(String[] args) {
    System.out
        .println("Answer Q1.Part C: The reason that we use decreasing frequency for terms in text collection is because "
            + "smaller integers are shorter than longer integers therefore"
            + "term indexes according to more frequent terms consume less bits"
            + " in memory => we save lots of space when representing text as sequence of integers");
  }
}
